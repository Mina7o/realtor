global:
  scrape_interval: 30s

scrape_configs:
  - job_name: 'realtor'
    metrics_path: /metrics
    static_configs:
      - targets: ['host.docker.internal:5001']
        labels:
          app: 'realtor-flask'
