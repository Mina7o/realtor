server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: realtor
    static_configs:
      - targets: [localhost]
        labels:
          job: realtor
          __path__: /logs/*.log
    pipeline_stages:
      - regex:
          expression: '^(?P<time>\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}) \| (?P<level>\w+) +\| (?P<module>\S+?) \| (?P<msg>.+)$'
      - labels:
          level:
          module:
